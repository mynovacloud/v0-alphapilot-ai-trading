"""Regression tests. Each test in this package is named for the bug it
guards against — see individual module docstrings for the original
incident. The set should never shrink; bugs that were once silent should
stay un-silenceable."""
